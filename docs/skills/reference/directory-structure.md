# Skill 目录结构参考

这里放 skill 目录结构规范。

推荐：

```text
skill-name/
  SKILL.md
  scripts/
  templates/
  references/
  assets/
```

原则：

- 入口必须清楚
- 长资料放 `references/`
- 可复用脚本放 `scripts/`
- 模板放 `templates/`
- 不要把所有内容塞进 `SKILL.md`

